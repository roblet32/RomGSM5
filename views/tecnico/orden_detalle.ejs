<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Detalle de Orden | ROCKETGSM</title>
  <link rel="stylesheet" href="/css/base.css">
  <link rel="stylesheet" href="/css/dashboard.css">
  <style>
    .detail-section {
      background: #fff;
      border-radius: var(--border-radius);
      box-shadow: 0 4px 24px rgba(0,0,0,0.07);
      padding: 24px;
      margin-bottom: 24px;
      border: 1px solid #e2e8f0;
    }
    .section-title {
      color: var(--primary-dark);
      font-size: 20px;
      font-weight: 600;
      margin-bottom: 16px;
      padding-bottom: 8px;
      border-bottom: 2px solid var(--primary);
    }
    .info-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 16px;
      margin-bottom: 16px;
    }
    .info-item {
      display: flex;
      flex-direction: column;
      gap: 4px;
    }
    .info-label {
      font-weight: 600;
      color: var(--primary-dark);
      font-size: 14px;
    }
    .info-value {
      color: var(--text);
      font-size: 16px;
      padding: 8px 12px;
      background: var(--background);
      border-radius: var(--border-radius);
      border: 1px solid #e2e8f0;
    }
    .status-badge {
      display: inline-block;
      padding: 6px 12px;
      border-radius: 20px;
      font-size: 12px;
      font-weight: bold;
      text-transform: uppercase;
    }
    .status-pendiente { 
      background: linear-gradient(135deg, #f39c12, #e67e22);
      color: white; 
    }
    .status-asignada { 
      background: linear-gradient(135deg, #3498db, #2980b9);
      color: white; 
    }
    .status-cotizacion_enviada { 
      background: linear-gradient(135deg, var(--secondary), #8e44ad);
      color: white; 
    }
    .status-aprobada { 
      background: linear-gradient(135deg, var(--success), #2ecc71);
      color: white; 
    }
    .status-en_proceso { 
      background: linear-gradient(135deg, #e67e22, #d35400);
      color: white; 
    }
    .status-finalizada { 
      background: linear-gradient(135deg, var(--success), #2ecc71);
      color: white; 
    }
    .status-cancelada { 
      background: linear-gradient(135deg, var(--danger), #c0392b);
      color: white; 
    }
    .photos-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 16px;
      margin-top: 12px;
    }
    .photo-item {
      border-radius: var(--border-radius);
      overflow: hidden;
      border: 2px solid #e2e8f0;
      transition: transform var(--transition);
    }
    .photo-item:hover {
      transform: scale(1.05);
      border-color: var(--primary);
    }
    .photo-item img {
      width: 100%;
      height: 500px;
      object-fit: cover;
    }
    .action-buttons {
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
      margin-top: 16px;
    }
    .btn-action {
      padding: 10px 20px;
      border-radius: var(--border-radius);
      text-decoration: none;
      display: inline-block;
      transition: all var(--transition);
      font-weight: 500;
    }
    .btn-take { 
      background: linear-gradient(135deg, var(--success), #2ecc71);
      color: white; 
    }
    .btn-quote { 
      background: linear-gradient(135deg, var(--primary), var(--secondary));
      color: white; 
    }
    .btn-finish { 
      background: linear-gradient(135deg, var(--success), #2ecc71);
      color: white; 
    }
    .btn-edit { 
      background: linear-gradient(135deg, #f39c12, #e67e22);
      color: white; 
    }
    .btn-action:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(0,0,0,0.15);
    }
    .form-group {
      margin-bottom: 16px;
    }
    .form-group label {
      display: block;
      font-weight: 600;
      color: var(--primary-dark);
      margin-bottom: 8px;
    }
    .form-group textarea {
      width: 100%;
      padding: 12px;
      border: 2px solid #e2e8f0;
      border-radius: var(--border-radius);
      font-family: inherit;
      font-size: 14px;
      transition: border-color var(--transition);
    }
    .form-group textarea:focus {
      outline: none;
      border-color: var(--primary);
    }
    .table-container {
      background: #fff;
      border-radius: var(--border-radius);
      box-shadow: 0 4px 24px rgba(0,0,0,0.07);
      overflow: hidden;
      border: 1px solid #e2e8f0;
      margin-top: 16px;
    }
    .table-container table {
      width: 100%;
      border-collapse: collapse;
    }
    .table-container th,
    .table-container td {
      padding: 16px;
      text-align: left;
      border-bottom: 1px solid #e2e8f0;
    }
    .table-container th {
      background: var(--background);
      font-weight: 600;
      color: var(--primary-dark);
    }
    .table-container tr:hover {
      background: var(--background);
    }
    .empty-state {
      text-align: center;
      padding: 40px;
      color: var(--text);
      background: #fff;
      border-radius: var(--border-radius);
      box-shadow: 0 4px 24px rgba(0,0,0,0.07);
      border: 1px solid #e2e8f0;
    }
    .empty-state p {
      font-size: 16px;
      margin: 0;
      color: var(--text);
    }
    .header-actions {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 24px;
      flex-wrap: wrap;
      gap: 16px;
    }
    .footer-actions {
      display: flex;
      gap: 12px;
      margin-top: 24px;
      flex-wrap: wrap;
    }
  </style>
</head>
<body>
  <main>
    <div class="container">
      <div class="header-actions">
        <h1>Detalle de Orden de Servicio</h1>
        <div class="action-buttons">
          <a href="/tecnico/dashboard" class="btn">Volver al Dashboard</a>
        </div>
      </div>

      <!-- Información del Cliente -->
      <div class="detail-section">
        <h2 class="section-title">📞 Información del Cliente</h2>
        <div class="info-grid">
          <div class="info-item">
            <span class="info-label">Nombre</span>
            <span class="info-value"><%= orden && orden.dispositivo && orden.dispositivo.cliente ? orden.dispositivo.cliente.nombre : 'N/A' %></span>
          </div>
          <div class="info-item">
            <span class="info-label">Teléfono</span>
            <span class="info-value"><%= orden && orden.dispositivo && orden.dispositivo.cliente && orden.dispositivo.cliente.telefono ? orden.dispositivo.cliente.telefono : 'N/A' %></span>
          </div>
          <div class="info-item">
            <span class="info-label">Email</span>
            <span class="info-value"><%= orden && orden.dispositivo && orden.dispositivo.cliente && orden.dispositivo.cliente.email ? orden.dispositivo.cliente.email : 'N/A' %></span>
          </div>
        </div>
      </div>

      <!-- Información del Dispositivo -->
      <div class="detail-section">
        <h2 class="section-title">💻 Información del Dispositivo</h2>
        <div class="info-grid">
          <div class="info-item">
            <span class="info-label">Tipo</span>
            <span class="info-value"><%= orden && orden.dispositivo ? orden.dispositivo.tipo : 'N/A' %></span>
          </div>
          <div class="info-item">
            <span class="info-label">Marca</span>
            <span class="info-value"><%= orden && orden.dispositivo ? orden.dispositivo.marca : 'N/A' %></span>
          </div>
          <div class="info-item">
            <span class="info-label">Modelo</span>
            <span class="info-value"><%= orden && orden.dispositivo ? orden.dispositivo.modelo : 'N/A' %></span>
          </div>
          <div class="info-item">
            <span class="info-label">Número de Serie</span>
            <span class="info-value"><%= orden && orden.dispositivo ? orden.dispositivo.numeroSerie : 'N/A' %></span>
          </div>
        </div>
        
        <div class="info-item" style="grid-column: 1 / -1;">
          <span class="info-label">Descripción del Problema</span>
          <span class="info-value"><%= orden && orden.dispositivo ? orden.dispositivo.descripcionProblema : 'N/A' %></span>
        </div>

        <% if (orden && orden.dispositivo && orden.dispositivo.fotos && orden.dispositivo.fotos.length > 0) { %>
          <div class="info-item" style="grid-column: 1 / -1;">
            <span class="info-label">Fotos del Dispositivo</span>
            <div class="photos-grid">
              <% orden.dispositivo.fotos.forEach(function(foto) { %>
                <div class="photo-item">
                  <img src="/uploads/<%= foto %>" alt="Foto del dispositivo" title="<%= foto %>">
                </div>
              <% }); %>
            </div>
          </div>
        <% } %>
      </div>

      <!-- Datos de la Orden -->
      <div class="detail-section">
        <h2 class="section-title">📋 Datos de la Orden</h2>
        <div class="info-grid">
          <div class="info-item">
            <span class="info-label">Tipo de Servicio</span>
            <span class="info-value"><%= orden && orden.tipoServicio ? orden.tipoServicio : 'N/A' %></span>
          </div>
          <div class="info-item">
            <span class="info-label">Prioridad</span>
            <span class="info-value"><%= orden && orden.prioridad ? orden.prioridad : 'N/A' %></span>
          </div>
          <div class="info-item">
            <span class="info-label">Estado</span>
            <span class="status-badge status-<%= orden && orden.estado ? orden.estado : 'pendiente' %>">
              <%= orden && orden.estado ? orden.estado.replace('_', ' ') : 'N/A' %>
            </span>
          </div>
          <div class="info-item">
            <span class="info-label">Técnico Asignado</span>
            <span class="info-value"><%= orden && orden.tecnicoAsignado && orden.tecnicoAsignado.nombre ? orden.tecnicoAsignado.nombre : 'N/A' %></span>
          </div>
          <div class="info-item">
            <span class="info-label">Fecha de Creación</span>
            <span class="info-value"><%= orden && orden.fechaCreacion ? new Date(orden.fechaCreacion).toLocaleString() : 'N/A' %></span>
          </div>
          <div class="info-item">
            <span class="info-label">Fecha de Asignación</span>
            <span class="info-value"><%= orden && orden.fechaAsignacion ? new Date(orden.fechaAsignacion).toLocaleString() : 'N/A' %></span>
          </div>
          <div class="info-item">
            <span class="info-label">Fecha de Inicio</span>
            <span class="info-value"><%= orden && orden.fechaInicio ? new Date(orden.fechaInicio).toLocaleString() : 'N/A' %></span>
          </div>
          <div class="info-item">
            <span class="info-label">Fecha de Finalización</span>
            <span class="info-value"><%= orden && orden.fechaFinalizacion ? new Date(orden.fechaFinalizacion).toLocaleString() : 'N/A' %></span>
          </div>
          <div class="info-item">
            <span class="info-label">Fecha de Entrega</span>
            <span class="info-value"><%= orden && orden.fechaEntrega ? new Date(orden.fechaEntrega).toLocaleString() : 'N/A' %></span>
          </div>
        </div>

        <% if (orden && orden.diagnosticoInicial) { %>
          <div class="info-item" style="grid-column: 1 / -1;">
            <span class="info-label">Diagnóstico Inicial</span>
            <span class="info-value"><%= orden.diagnosticoInicial %></span>
          </div>
        <% } %>

        <% if (orden && orden.observaciones) { %>
          <div class="info-item" style="grid-column: 1 / -1;">
            <span class="info-label">Observaciones</span>
            <span class="info-value"><%= orden.observaciones %></span>
          </div>
        <% } %>

        <% if (orden && orden.trabajoRealizado) { %>
          <div class="info-item" style="grid-column: 1 / -1;">
            <span class="info-label">Trabajo Realizado</span>
            <span class="info-value"><%= orden.trabajoRealizado %></span>
          </div>
        <% } %>

        <!-- Acciones según el estado -->
        <div class="action-buttons">
          <% if (orden && orden.estado === 'pendiente') { %>
            <form action="/tecnico/ordenes/<%= orden._id %>/tomar" method="POST" style="display: inline;">
              <button type="submit" class="btn-action btn-take">📋 Tomar Orden</button>
            </form>
          <% } %>
          
          <% if (orden && orden.estado === 'asignada') { %>
            <a href="/tecnico/ordenes/<%= orden._id %>/cotizar" class="btn-action btn-quote">💰 Crear Cotización</a>
          <% } %>
          
          <% if (orden && (orden.estado === 'aprobada' || orden.estado === 'en_proceso')) { %>
            <form action="/tecnico/ordenes/<%= orden._id %>/finalizar" method="POST" style="display: inline;">
              <div class="form-group">
                <label for="trabajoRealizado">Trabajo Realizado:</label>
                <textarea name="trabajoRealizado" id="trabajoRealizado" rows="4" required placeholder="Describe el trabajo realizado..."></textarea>
              </div>
              <button type="submit" class="btn-action btn-finish">✅ Finalizar Orden</button>
            </form>
          <% } %>
        </div>
      </div>

      <!-- Cotizaciones -->
      <div class="detail-section">
        <h2 class="section-title">💰 Cotizaciones</h2>
        
        <% if (!cotizaciones || cotizaciones.length === 0) { %>
          <div class="empty-state">
            <p>No hay cotizaciones registradas para esta orden.</p>
          </div>
        <% } else { %>
          <div class="table-container">
            <table>
              <thead>
                <tr>
                  <th>Fecha</th>
                  <th>Estado</th>
                  <th>Total</th>
                  <th>Acciones</th>
                </tr>
              </thead>
              <tbody>
                <% cotizaciones.forEach(function(c) { %>
                  <tr>
                    <td><%= c && c.fechaCreacion ? new Date(c.fechaCreacion).toLocaleString() : 'N/A' %></td>
                    <td>
                      <span class="status-badge status-<%= c && c.estado ? c.estado : 'pendiente' %>">
                        <%= c && c.estado ? c.estado.replace('_', ' ') : 'N/A' %>
                      </span>
                    </td>
                    <td><strong>$<%= c && c.total ? c.total.toFixed(2) : '0.00' %></strong></td>
                    <td>
                      <% if (
                        c.estado === 'rechazada' &&
                        c.creadoPor &&
                        ((typeof c.creadoPor === 'object' && c.creadoPor !== null && c.creadoPor._id && c.creadoPor._id.toString() === user._id.toString()) ||
                        (typeof c.creadoPor === 'string' && c.creadoPor === user._id.toString()))
                      ) { %>
                        <a href="/tecnico/cotizaciones/<%= c._id %>/editar" class="btn-action btn-edit">✏️ Editar</a>
                      <% } %>
                    </td>
                  </tr>
                <% }); %>
              </tbody>
            </table>
          </div>
        <% } %>
      </div>

      <div class="footer-actions">
        <a href="/tecnico/mis-ordenes" class="btn">Mis Órdenes</a>
        <a href="/tecnico/dashboard" class="btn">Dashboard</a>
      </div>
    </div>
  </main>
</body>
</html> 